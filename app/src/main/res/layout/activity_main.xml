<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#f44538"
    tools:context="yuvabharat.ramu.bloodonator.MainActivity">

    <ImageView
        android:id="@+id/logo"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_centerHorizontal="true"
        android:scaleType="centerInside"
        android:src="@mipmap/ic_heart_two_hand"
        android:contentDescription="@string/logo_of_the_app" />

    <TextView
        android:id="@+id/DonateText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/logo"
        android:gravity="center"
        android:text="@string/donate_to_save_life"
        android:textColor="#fff"
        android:textSize="40sp" />
    <TextView
        android:id="@+id/slogun"
        android:textColor="#fff"
        android:gravity="center"
        android:text="@string/slogon_1"
        android:layout_marginTop="10dp"
        android:layout_below="@+id/DonateText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />
    <com.google.android.gms.common.SignInButton
        android:id="@+id/signin"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_alignParentBottom="true"
        android:layout_margin="20dp" />
</RelativeLayout>
